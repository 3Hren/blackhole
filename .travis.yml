os:
    - linux
    - osx

env:
    - SUITE=tests
    - SUITE=examples

language:
    - cpp

before_install:
    - ./ci/before_install.sh

install:
    - ./ci/install.sh

before_script:
    - mkdir build && cd build

script:
    - ../ci/script.sh

#ifndef CONFIG_H
#define CONFIG_H

#define BLACKHOLE_VERSION_MAJOR ${BLACKHOLE_VERSION_MAJOR}
#define BLACKHOLE_VERSION_MINOR ${BLACKHOLE_VERSION_MINOR}
#define BLACKHOLE_VERSION_PATCH ${BLACKHOLE_VERSION_PATCH}
#cmakedefine BLACKHOLE_HEADER_ONLY
#cmakedefine BLACKHOLE_DEBUG

#if defined(BLACKHOLE_HEADER_ONLY)
    #define BLACKHOLE_API inline
#endif

// If BLACKHOLE_API isn't defined yet define it now.
#if !defined(BLACKHOLE_API)
    #define BLACKHOLE_API
#endif

#endif // CONFIG_H
